#!da

// Just a da-lang sample code.

function foo(flag) {
  x = 6
  y = 4
  z = 2

  if flag || x > y && y > z {
    a = 100
    b = 99
    return x + y / z * (a - b)
  } else if x == y {
    return (x - y) * 2
  } else {
    return 0
  }
}

foo(true) # Call foo function directly.