#!da

// Just a da-lang sample code.

function foo(flag) {
  x = 6
  y = 4
  z = 2

  if flag || x > y && y > z {
    return x + y / z
  } else {
    return (x - y) * 2
  }
}

<: foo(true) # Call foo function directly.