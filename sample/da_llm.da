#!dalang

graph net(x, y) {
    z = ops.matmul(x, y)
    return ops.add(z, z)
}

function main() {
    "a = int(0)
    b = 1 : int
    if a > b {
        <: 'error' <: '\n'
    }"

    'x = tensor((2, 3))
    y = tensor((3, 4))'
    x = tensor(2)
    y = tensor(3)
    'x = 2
    y = 3'
    return net(x, y)
}

<: main() <: '\n'