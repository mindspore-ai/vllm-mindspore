OPERATOR(Equal)        // ==
OPERATOR(NotEqual)     // !=
OPERATOR(GreaterThan)  // >
OPERATOR(LessThan)     // <
OPERATOR(GreaterEqual) // >=
OPERATOR(LessEqual)    // <=
OPERATOR(LogicalOr)    // ||
OPERATOR(LogicalAnd)   // &&
OPERATOR(ShiftRight)   // >>
OPERATOR(ShiftLeft)    // <<
OPERATOR(AddAssign)    // +=
OPERATOR(SubAssign)    // -=
OPERATOR(MulAssign)    // *=
OPERATOR(DivAssign)    // /=
OPERATOR(ModAssign)    // %=
OPERATOR(Assign)       // =
OPERATOR(Add)          // +
OPERATOR(Sub)          // -
OPERATOR(Mul)          // *
OPERATOR(Div)          // /
OPERATOR(Mod)          // %
OPERATOR(StdCin)       // <:
OPERATOR(StdCout)      // >: